<UserControl x:Class="AMPManager.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:oxy="http://oxyplot.org/wpf" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1280"
             Background="{StaticResource AppBackground}">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Style="{StaticResource PanelStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="로그 목록 (Timestamp)" Style="{StaticResource TitleText}"/>
                <ListBox Grid.Row="1" 
                         ItemsSource="{Binding TimestampList}" 
                         Style="{StaticResource ModernListBox}"
                         ItemContainerStyle="{StaticResource ModernListBoxItem}"/>

                <TextBlock Grid.Row="2" Text="불량 메시지" Style="{StaticResource TitleText}" Foreground="#FF5252"/>
                <ListBox Grid.Row="3" 
                         ItemsSource="{Binding DefectMessageList}" 
                         Style="{StaticResource ModernListBox}"
                         ItemContainerStyle="{StaticResource ModernListBoxItem}"/>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="300"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource PanelStyle}" Margin="0,5,5,5">
                    <Grid>
                        <Image Source="{Binding CameraImage1}" Stretch="Uniform"/>
                        <TextBlock Text="CAM 1 (OFF)" Foreground="{StaticResource SecondaryText}" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CameraImage1}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource PanelStyle}" Margin="5,5,0,5">
                    <Grid>
                        <Image Source="{Binding CameraImage2}" Stretch="Uniform"/>
                        <TextBlock Text="CAM 2 (OFF)" Foreground="{StaticResource SecondaryText}" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CameraImage2}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource PanelStyle}">
                <Grid Margin="15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="10">
                        <TextBlock Text="작업 진행률" Style="{StaticResource TitleText}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        <Grid Margin="0,30,0,0">
                            <oxy:PlotView Model="{Binding WorkPieModel}" Background="Transparent"/>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                                <TextBlock Text="{Binding AllocationPercent, StringFormat={}{0:F1}%}" 
                                           FontSize="36" FontWeight="Bold" Foreground="{StaticResource AccentColor}" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="COMPLETED" FontSize="10" Foreground="{StaticResource SecondaryText}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <Grid Grid.Column="1" Margin="10">
                        <TextBlock Text="실시간 불량률" Style="{StaticResource TitleText}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                        <Grid Margin="0,30,0,0">
                            <oxy:PlotView Model="{Binding DefectPieModel}" Background="Transparent"/>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                                <TextBlock Text="{Binding DefectRate, StringFormat={}{0:F1}%}" 
                                           FontSize="36" FontWeight="Bold" Foreground="#FF5252" 
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="DEFECT RATE" FontSize="10" Foreground="{StaticResource SecondaryText}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Grid>

                    <Border Grid.Column="2" Background="#3A3A4A" CornerRadius="5" Padding="15" Margin="10" VerticalAlignment="Center">
                        <StackPanel>
                            <Grid Margin="0,0,0,15">
                                <TextBlock Text="목표 할당량" Foreground="{StaticResource SecondaryText}" FontSize="12"/>
                                <TextBlock Text="{Binding AllocationCount}" FontWeight="Bold" Foreground="White" FontSize="18" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,15">
                                <TextBlock Text="현재 완료" Foreground="{StaticResource SecondaryText}" FontSize="12"/>
                                <TextBlock Text="{Binding CurrentComplete}" FontWeight="Bold" Foreground="{StaticResource AccentColor}" FontSize="18" HorizontalAlignment="Right"/>
                            </Grid>
                            <Separator Background="#505060" Margin="0,0,0,15"/>
                            <Grid>
                                <TextBlock Text="불량률" Foreground="{StaticResource SecondaryText}" FontSize="12"/>
                                <TextBlock Text="{Binding DefectRate, StringFormat={}{0:F1}%}" FontWeight="Bold" Foreground="#FF5252" FontSize="18" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
<Window x:Class="AMPManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AMPManager.ViewModel"
        xmlns:v="clr-namespace:AMPManager.View"
        mc:Ignorable="d" Title="AMP Manager" Height="720" Width="1280" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Grid Background="#4A148C">
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type vm:HomeViewModel}">
                <v:HomeView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:LogViewModel}">
                <v:LogView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <v:SettingsView/>
            </DataTemplate>
            <Style x:Key="NavBtn" TargetType="Button">
                <Setter Property="Background" Value="#6A1B9A"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Margin" Value="10"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </Grid.Resources>
        <Border Grid.Row="0" Grid.Column="1" Background="#353560">
            <DockPanel LastChildFill="False">
                <TextBlock Text="계측 프로그램" 
           VerticalAlignment="Center" 
           Margin="20,0" 
           FontSize="24" 
           FontWeight="Bold" 
           Foreground="White"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="20,0">
                    <Button Content="시작" Command="{Binding StartCommand}" Width="100" Height="40" Background="#5070D0" Foreground="White"/>
                    <Button Content="정지" Command="{Binding StopCommand}" Width="100" Height="40" Background="#E53935" Foreground="White" Margin="10,0,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>
        <StackPanel Grid.Row="1" Grid.Column="0" Background="#303050">
            <Button Content="메인" Command="{Binding NavigateCommand}" CommandParameter="Main" Style="{StaticResource NavBtn}"/>
            <Button Content="로그" Command="{Binding NavigateCommand}" CommandParameter="Log" Style="{StaticResource NavBtn}"/>
            <Button Content="설정" Command="{Binding NavigateCommand}" CommandParameter="Settings" Style="{StaticResource NavBtn}"/>
        </StackPanel>
        <Border Grid.Row="1" Grid.Column="1" Background="#202040">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Border>
    </Grid>
</Window>
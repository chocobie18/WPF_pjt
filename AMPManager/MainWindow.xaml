<Window x:Class="AMPManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:AMPManager.ViewModel"
        xmlns:v="clr-namespace:AMPManager.View"
        Title="AMP Manager" Height="768" Width="1366" 
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource AppBackground}">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:HomeViewModel}">
            <v:HomeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:LogViewModel}">
            <v:LogView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <v:SettingsView/>
        </DataTemplate>

        <Style x:Key="NavBtnStyle" TargetType="Button">
            <Setter Property="Height" Value="60"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SecondaryText}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="#353545"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBackground}">
            <DockPanel LastChildFill="False">
                <TextBlock Text="계측 모니터링 시스템" VerticalAlignment="Center" Margin="20,0" FontSize="20" FontWeight="Bold" Foreground="{StaticResource AccentColor}"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Center" Margin="20,0">
                    <Button Content="시스템 시작" Command="{Binding StartCommand}" Width="100" Height="36" Background="{StaticResource AccentColor}" Foreground="White" BorderThickness="0" FontWeight="Bold" Margin="0,0,10,0"/>
                    <Button Content="정지" Command="{Binding StopCommand}" Width="100" Height="36" Background="#E53935" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Background="#252530">

                <Button Content="HOME" Command="{Binding NavigateCommand}" CommandParameter="Main">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtnStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentViewName}" Value="Main">
                                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Background" Value="#2F2F3D"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="LOG" Command="{Binding NavigateCommand}" CommandParameter="Log">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtnStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentViewName}" Value="Log">
                                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Background" Value="#2F2F3D"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="SET" Command="{Binding NavigateCommand}" CommandParameter="Settings">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource NavBtnStyle}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentViewName}" Value="Settings">
                                    <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Background" Value="#2F2F3D"/>
                                    <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                                    <Setter Property="BorderThickness" Value="3,0,0,0"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <Border Grid.Column="1" Background="Transparent" Padding="0">
                <ContentControl Content="{Binding CurrentViewModel}"/>
            </Border>
        </Grid>
    </Grid>
</Window>